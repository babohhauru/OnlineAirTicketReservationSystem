spring:
  thymeleaf:
    mode: HTML
    prefix:
      cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/airline?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: violet
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

